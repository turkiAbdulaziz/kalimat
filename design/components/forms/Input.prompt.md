The only text-entry control — sign-in email and display name. Label above, hint below, both optional.

\`\`\`jsx
<Input label="البريد الإلكتروني" type="email" value={email} onChange={setEmail} placeholder="name@example.com" />
\`\`\`

Invalid state is a taupe border plus taupe hint text — never red. Focus is the brown ring.
