The settings primitive — it includes its own label row and hairline divider, so stack them directly.

\`\`\`jsx
<Switch label="الوضع الليلي" hint="خلفية بنية غامقة" checked={dark} onChange={setDark} />
\`\`\`
