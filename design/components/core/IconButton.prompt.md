Header-only icon affordance; renders a Lucide glyph (the substituted icon set — load lucide from CDN on the page).

\`\`\`jsx
<IconButton icon="bar-chart-3" label="الإحصائيات" onClick={openStats} />
\`\`\`
