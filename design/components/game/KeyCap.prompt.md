A single keyboard cap. Prefer \`Keyboard\` unless you need one key in isolation (e.g. a how-to-play diagram).

\`\`\`jsx
<KeyCap label="ح" state="present" onPress={onKey} />
<KeyCap label="إدخال" wide onPress={submit} />
\`\`\`
